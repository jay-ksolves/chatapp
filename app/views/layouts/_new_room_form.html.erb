<%= form_with(model: @room, remote: true,class:"d-flex ") do |f| %>
  <%= f.text_field :name, class: " form-control", autocomplete: 'off' ,placeholder: "Room name " %>
  <%= f.submit data: { disable_with: false } ,class: "btn btn-success  btn-sm  spa" ,style: "margin-left:10px"
    %>
    <% end %>